# CODEBASE.md — Living Context File
_Generated by Brownfield Cartographer on 2026-03-14 21:33 UTC_
---
## Architecture Overview
The system is a document processing and conversion tool, primarily focused on converting Markdown files containing Mermaid diagrams into PDFs. It consists of key modules such as `scripts/md_to_pdf.py`, which handles the conversion logic, and testing modules like `tests/test_navigator.py` and `tests/test_semanticist.py`, which validate the functionality of the `Navigator` agent and various semantic analysis tools, respectively. The system's data flow begins with Markdown files as input, which are processed by `md_to_pdf.py` to extract and render Mermaid diagrams, resulting in PDF outputs. The testing modules ensure that the conversion process and semantic analysis tools operate correctly, providing a robust architecture for document transformation and validation.
---
## Critical Path (Top 5 Modules by PageRank)
- **`backup/templates/pdf-metadata.yaml`** (pagerank=0.0141)  
  _(no purpose statement)_
- **`backup/templates/pdf-metadata-final.yaml`** (pagerank=0.0141)  
  _(no purpose statement)_
- **`scripts/md_to_pdf.py`** (pagerank=0.0141)  
  This script converts Markdown files containing Mermaid diagrams to PDFs. It extracts Mermaid code blocks, renders them as images using Mermaid CLI, and replaces the blocks with image references before running Pandoc to generate the final PDF document.
- **`tests/test_navigator.py`** (pagerank=0.0141)  
  This module tests the `Navigator` agent and its tools, including `find_implementation`, `blast_radius`, and `trace_lineage`. It ensures the agent can correctly identify module implementations based on purpose statements, trace dependencies in a graph, and query the codebase using language models. The tests validate the functionality of these tools in the context of a Python codebase with modules for data ingestion, transformation, and serving.
- **`tests/test_semanticist.py`** (pagerank=0.0141)  
  This module tests various functions in the `src/agents/semanticist.py` file, ensuring they correctly extract docstrings, generate purpose statements using a language model, cluster code into domains, detect docstring drift, and answer day-one questions about code modules. The tests utilize mock language model responses to verify the functionality without making actual API calls.

---
## Data Sources & Sinks
**Sources (entry points):**
- _(none detected)_

**Sinks (output endpoints):**
- _(none detected)_

---
## Domain Architecture Map
**Code analysis testing** (13 modules):
- `src/agents/hydrologist.py`
- `src/models/graph.py`
- `tests/test_archivist.py`
- `tests/test_dag_config_parser.py`
- `tests/test_knowledge_graph.py`
- `tests/test_llm_config.py`
- `tests/test_models_edges.py`
- `tests/test_navigator.py`
- _…and 5 more_

**Codebase analysis** (3 modules):
- `scripts/md_to_pdf.py`
- `src/agents/surveyor.py`
- `src/cli.py`

**Unknown** (3 modules):
- `backup/templates/pdf-metadata-final.yaml`
- `backup/templates/pdf-metadata.yaml`
- `tests/__init__.py`


---
## Known Debt
- No circular dependencies detected.

**Documentation drift (docstring contradicts implementation):**
- `tests/test_navigator.py`: The docstring is too broad and does not specify the tools and functionalities tested, which is provided in the code comments.
- `tests/test_semanticist.py`: The docstring does not accurately reflect the actual purpose of the module, which includes testing functionality such as extracting docstrings, generating purpose statements, clustering code into domains, detecting docstring drift, and answering day-one questions. The documentations only mentions tests for purpose statements, doc drift, and domain clustering.
- `tests/test_archivist.py`: The docstring mentions tests for `CODEBASE.md`, `onboarding brief`, and `trace logger`, while the actual purpose includes generating documentation, onboarding briefs, and trace logs based on codebase analysis, as well as testing the `TraceLogger` class, graph helper functions, and the `archive` function.
- `tests/test_sql_lineage.py`: The docstring is too vague and does not provide details about the specific functionality and purpose of the unit tests in `tests/test_sql_lineage.py`.
- `tests/test_knowledge_graph.py`: The docstring is overly broad and does not accurately describe the specific classes and operations tested in `tests/test_knowledge_graph.py`.
- `tests/test_dag_config_parser.py`: The docstring states that the tests are for `src/analyzers/dag_config_parser.py`, but the actual purpose described in the code comments is for testing configuration files used in data engineering projects, specifically for Airflow DAGs and dbt projects.
- `tests/test_llm_config.py`: The docstring is too vague and does not provide details about the specific functionalities and tests covered in `tests/test_llm_config.py`.
- `tests/test_models_edges.py`: The document string is too vague and does not specify the actual focus on testing edge types, weights, and properties, and node attributes.
- `tests/test_tree_sitter_analyzer.py`: The docstring states that the tests are for `src/analyzers/tree_sitter_analyzer.py`, but the actual purpose describes a suite of tests for language detection, syntax parsing, and code analysis for multiple programming languages.
- `tests/test_python_data_flow.py`: The docstring states that the tests are for `src/analyzers/python_data_flow.py`, but the actual code does not appear to be a test module. It is likely a module for extracting data flow operations from Python code.

---
## High-Velocity Files (Pain Points)
- `src/cli.py` — 4 commits (last 30d)
- `tests/test_llm_config.py` — 3 commits (last 30d)
- `src/agents/hydrologist.py` — 3 commits (last 30d)
- `tests/test_sql_lineage.py` — 2 commits (last 30d)
- `tests/test_knowledge_graph.py` — 2 commits (last 30d)
- `tests/test_models_edges.py` — 2 commits (last 30d)
- `tests/test_tree_sitter_analyzer.py` — 2 commits (last 30d)
- `src/agents/surveyor.py` — 2 commits (last 30d)
- `scripts/md_to_pdf.py` — 1 commits (last 30d)
- `tests/test_navigator.py` — 1 commits (last 30d)

---
## Module Purpose Index
- **`scripts/md_to_pdf.py`**: This script converts Markdown files containing Mermaid diagrams to PDFs. It extracts Mermaid code blocks, renders them as images using Mermaid CLI, and replaces the blocks with image references before running Pandoc to generate the final PDF document.
- **`src/agents/hydrologist.py`**: The `hydrologist.py` module extracts data lineage from a codebase containing SQL, dbt, Airflow, and Python data flow. It builds a `LineageGraph` by analyzing these components and writing the graph to a JSON file, enabling developers to understand data flow and dependencies within their project.
- **`src/agents/surveyor.py`**: The `Surveyor Agent` performs static structure analysis on a repository, building a module graph that includes information about module dependencies, git velocity, and architectural metrics like PageRank and strongly connected components. It uses file extensions to identify analyzable files, resolves import paths, computes change frequencies, and identifies potential dead code, ultimately outputting a JSON representation of the module graph.
- **`src/cli.py`**: This module serves as the command-line interface (CLI) for the Brownfield Cartographer, a tool designed to map the architecture and data lineage of existing codebases. It provides two main subcommands: `analyze` for running a full analysis pipeline (Surveyor + Hydrologist + optional LLM stages), and `query` for an interactive mode over existing `.cartography/` artifacts, allowing users to explore and query the analyzed data.
- **`src/models/graph.py`**: The `graph.py` module defines schemas for serializing two types of graphs: the module import graph and the data lineage graph. It uses Pydantic models to represent these graphs, with nodes and edges structured as dictionaries. The module provides specific serialization for each graph type, including the types of nodes and edges, which are crucial for tracking dependencies and data flows within applications.
- **`tests/test_archivist.py`**: This test suite validates the functionality of various components in the `src/agents/archivist.py` module, ensuring that it can generate documentation, onboarding briefs, and trace logs based on codebase analysis. It includes tests for the `TraceLogger` class, graph helper functions like `_top_modules_by_pagerank`, and the `archive` function, which orchestrates the generation of artifacts using an LLM for insights.
- **`tests/test_dag_config_parser.py`**: This module contains tests for parsing and analyzing configuration files used in data engineering projects, specifically for Airflow DAGs and dbt projects. It includes functions to parse YAML files, extract model and source definitions, and build a project structure, ensuring that the configuration files are correctly interpreted and used in the data processing workflow.
- **`tests/test_knowledge_graph.py`**: The `tests/test_knowledge_graph.py` file contains unit tests for the `ModuleGraph` and `LineageGraph` classes. These classes manage and manipulate a knowledge graph representing software modules, functions, and their relationships, including dependencies and data flow. The tests verify the correctness of operations such as adding modules and functions, establishing edges, and calculating lineage properties like sources, sinks, and blast radii.
- **`tests/test_llm_config.py`**: The `tests/test_llm_config.py` module provides unit tests for the `src/llm_config.py` module, which manages configuration for language model providers, including setting up default configurations, handling model names, and loading configurations from environment variables and TOML files. The tests ensure that the configuration logic is correct and that different providers and models are handled appropriately.
- **`tests/test_models_edges.py`**: This module tests the data models and validation logic for edges and nodes in a software architecture graph, ensuring correct edge types, weights, and properties are maintained while validating node attributes and raising appropriate errors for invalid data.
- **`tests/test_navigator.py`**: This module tests the `Navigator` agent and its tools, including `find_implementation`, `blast_radius`, and `trace_lineage`. It ensures the agent can correctly identify module implementations based on purpose statements, trace dependencies in a graph, and query the codebase using language models. The tests validate the functionality of these tools in the context of a Python codebase with modules for data ingestion, transformation, and serving.
- **`tests/test_orchestrator.py`**: The `tests/test_orchestrator.py` module contains unit tests for the `src/orchestrator.py` file, which is responsible for orchestrating the pipeline and handling incremental updates in a repository. The tests validate the functionality of various helper functions and the main `run` function, ensuring that they correctly resolve repository paths, determine changed files, and save run metadata.
- **`tests/test_python_data_flow.py`**: The module `python_data_flow` extracts data flow operations from Python code, specifically focusing on pandas reads and writes, PySpark patterns, and SQLAlchemy execute calls. It returns a list of dictionaries detailing the type of operation (read or write), the method used, the dataset involved, and the line number where the operation occurs.
- **`tests/test_semanticist.py`**: This module tests various functions in the `src/agents/semanticist.py` file, ensuring they correctly extract docstrings, generate purpose statements using a language model, cluster code into domains, detect docstring drift, and answer day-one questions about code modules. The tests utilize mock language model responses to verify the functionality without making actual API calls.
- **`tests/test_sql_lineage.py`**: The `tests/test_sql_lineage.py` file contains unit tests for the `src/analyzers/sql_lineage.py` module, which analyzes SQL files to extract table dependencies and line ranges. The module includes functions to parse SQL files, extract dependencies, and analyze directories containing SQL files, ensuring accurate identification of table sources and targets within SQL queries.
- **`tests/test_tree_sitter_analyzer.py`**: This module provides a suite of unit tests for the `tree_sitter_analyzer.py` file, which includes functionalities such as language detection, syntax parsing, and code analysis for different programming languages like Python, YAML, JavaScript, TypeScript, and SQL. The tests cover various aspects such as line and comment counting, class and function extraction, import handling, and SQL table extraction, ensuring the analyzer works correctly for different types of source code.
