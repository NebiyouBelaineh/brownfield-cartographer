# CODEBASE.md — Living Context File
_Generated by Brownfield Cartographer on 2026-03-13 15:34 UTC_
---
## Architecture Overview
The system appears to be a data processing and transformation pipeline, likely using a toolchain that includes dbt for data modeling and transformation, and GitHub Actions for continuous integration and deployment. The primary modules, such as `dbt_project.yml`, define the project's configuration and settings for dbt, while `Taskfile.yml` and `.github/workflows/cd_prod.yml` manage the automation and deployment processes. The data sources include various SQL queries and transformed tables, with the sinks being staging tables (`stg_locations`, `stg_orders`, `stg_order_items`, `stg_products`, `stg_supplies`). The system flows data from raw sources through transformations and modeling to produce ready-to-use datasets for further analysis or reporting.
---
## Critical Path (Top 5 Modules by PageRank)
- **`packages.yml`** (pagerank=0.0000)  
  _(no purpose statement)_
- **`Taskfile.yml`** (pagerank=0.0000)  
  _(no purpose statement)_
- **`package-lock.yml`** (pagerank=0.0000)  
  _(no purpose statement)_
- **`dbt_project.yml`** (pagerank=0.0000)  
  _(no purpose statement)_
- **`.github/workflows/cd_prod.yml`** (pagerank=0.0000)  
  _(no purpose statement)_

---
## Data Sources & Sinks
**Sources (entry points):**
- `renamed`
- `source`
- `/home/neba/tenx/week4/jaffle-shop/models/marts/metricflow_time_spine.sql:metricflow_time_spine`
- `order_supplies_summary`
- `joined`
- `customer_orders_summary`
- `customer_order_count`
- `compute_booleans`
- `order_items_summary`

**Sinks (output endpoints):**
- `stg_locations`
- `stg_orders`
- `stg_order_items`
- `stg_products`
- `stg_supplies`
- `stg_customers`
- `metricflow_time_spine`

---
## Domain Architecture Map
**Monitoring** (1 modules):
- `.github/workflows/scripts/dbt_cloud_run_job.py`

**Unknown** (21 modules):
- `.github/workflows/cd_prod.yml`
- `.github/workflows/cd_staging.yml`
- `.github/workflows/ci.yml`
- `.pre-commit-config.yaml`
- `Taskfile.yml`
- `dbt_project.yml`
- `models/marts/customers.yml`
- `models/marts/locations.yml`
- _…and 13 more_


---
## Known Debt
- No circular dependencies detected.

- No documentation drift detected.

---
## High-Velocity Files (Pain Points)
- _(git velocity data unavailable — not a git repo or no recent commits)_

---
## Module Purpose Index
- **`.github/workflows/scripts/dbt_cloud_run_job.py`**: This script triggers a dbt job on the dbt Cloud platform using an API call and monitors its execution status until completion. It is designed to integrate seamlessly into a GitHub Actions workflow to automate the execution of dbt jobs based on code pushes or other events.
