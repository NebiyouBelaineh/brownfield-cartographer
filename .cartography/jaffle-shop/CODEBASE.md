# CODEBASE.md — Living Context File
_Generated by Brownfield Cartographer on 2026-03-14 14:37 UTC_
---
## Architecture Overview
This system appears to be a data engineering and pipeline automation tool, likely for a data warehouse or analytics platform. The core components include a CI/CD pipeline managed through a Python script (`dbt_cloud_run_job.py`), which interacts with a dbt project configured in `dbt_project.yml`. The system utilizes YAML files (`packages.yml`, `Taskfile.yml`, `package-lock.yml`) for managing dependencies and tasks. Data is sourced from various SQL models and sinks into staging tables (`stg_locations`, `stg_orders`, `stg_order_items`, `stg_products`, `stg_supplies`). The architecture facilitates automated data processing and monitoring through the dbt pipeline, triggered and managed by the Python script within a CI/CD environment.
---
## Critical Path (Top 5 Modules by PageRank)
- **`.github/workflows/scripts/dbt_cloud_run_job.py`** (pagerank=0.0455)  
  This Python script automates the process of triggering and monitoring a dbt job in a CI/CD pipeline. It uses environment variables to configure the job details and the dbt Cloud API to run the job, check its status, and handle retries until the job completes successfully or fails.
- **`packages.yml`** (pagerank=0.0455)  
  _(no purpose statement)_
- **`Taskfile.yml`** (pagerank=0.0455)  
  _(no purpose statement)_
- **`package-lock.yml`** (pagerank=0.0455)  
  _(no purpose statement)_
- **`dbt_project.yml`** (pagerank=0.0455)  
  _(no purpose statement)_

---
## Data Sources & Sinks
**Sources (entry points):**
- `renamed`
- `source`
- `/home/neba/tenx/week4/repos/jaffle-shop/models/marts/metricflow_time_spine.sql:metricflow_time_spine`
- `order_supplies_summary`
- `joined`
- `customer_orders_summary`
- `customer_order_count`
- `compute_booleans`
- `order_items_summary`

**Sinks (output endpoints):**
- `stg_locations`
- `stg_orders`
- `stg_order_items`
- `stg_products`
- `stg_supplies`
- `stg_customers`
- `metricflow_time_spine`

---
## Domain Architecture Map
**Monitoring** (1 modules):
- `.github/workflows/scripts/dbt_cloud_run_job.py`

**Unknown** (21 modules):
- `.github/workflows/cd_prod.yml`
- `.github/workflows/cd_staging.yml`
- `.github/workflows/ci.yml`
- `.pre-commit-config.yaml`
- `Taskfile.yml`
- `dbt_project.yml`
- `models/marts/customers.yml`
- `models/marts/locations.yml`
- _…and 13 more_


---
## Known Debt
- No circular dependencies detected.

- No documentation drift detected.

---
## High-Velocity Files (Pain Points)
- _(git velocity data unavailable — not a git repo or no recent commits)_

---
## Module Purpose Index
- **`.github/workflows/scripts/dbt_cloud_run_job.py`**: This Python script automates the process of triggering and monitoring a dbt job in a CI/CD pipeline. It uses environment variables to configure the job details and the dbt Cloud API to run the job, check its status, and handle retries until the job completes successfully or fails.
