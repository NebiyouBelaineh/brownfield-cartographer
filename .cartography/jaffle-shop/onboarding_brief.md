# FDE Day-One Onboarding Brief

_Generated by Brownfield Cartographer — 2026-03-14 18:50 UTC_

---
## The Five FDE Day-One Questions

### Q1: What is the primary data ingestion path?

What is the primary data ingestion path?**

The primary data ingestion path flows through dbt source definitions. Per `models/staging/__sources.yml`, raw data sources are ingested into staging models including `stg_customers`, `stg_products`, `stg_locations`, `stg_supplies`, and `stg_orders` (referenced in the PageRank hub list). These staging models transform raw source data into standardized intermediate tables that feed downstream marts.

**

### Q2: What are the 3-5 most critical output datasets/endpoints?

What are the 3-5 most critical output datasets/endpoints?**

Based on data lineage sinks (out-degree=0 nodes), the most critical output is `metricflow_time_spine` (`models/marts/metricflow_time_spine.sql`), which serves as a semantic layer time dimension. Secondary critical outputs derived from high-PageRank marts modules include:
- `models/marts/order_items.yml` 
- `models/marts/products.yml`
- `models/marts/supplies.yml`
- `models/marts/locations.yml`

These mart-layer datasets appear to support business intelligence and reporting.

**

### Q3: What is the blast radius if the most critical module fails?

What is the blast radius if the most critical module fails?**

The architectural context shows **zero circular dependencies**, which significantly limits blast radius. However, if `metricflow_time_spine` (the sole sink with out-degree=0) fails, any metric layer or semantic layer queries depending on time dimensions would break. The staging layer (`models/staging/`) feeds all marts, so failure there would cascade to all downstream `models/marts/` outputs. The dbt Cloud orchestration script (`.github/workflows/scripts/dbt_cloud_run_job.py`) is a critical hub; if it fails, the entire CI/CD pipeline stalls.

**

### Q4: Where is the business logic concentrated vs distributed?

Where is the business logic concentrated vs distributed?**

Business logic is **concentrated in the marts layer** (`models/marts/*.yml`), which represents the dimensional/fact table structure. Transformation logic is **distributed across staging models** (`models/staging/stg_*.yml`) that handle entity-specific transformations. The `dbt_project.yml` and `packages.yml` suggest additional distributed logic via dbt packages and macro libraries. The context does not provide SQL model files, so specific business rule locations cannot be identified.

**

### Q5: What has changed most frequently in the last 90 days (high-velocity files)?

What has changed most frequently in the last 90 days (high-velocity files)?**

The provided data shows **zero commits** across all high-velocity file candidates listed (`.pre-commit-config.yaml`, `.github/workflows/scripts/dbt_cloud_run_job.py`, `packages.yml`, `dbt_project.yml`, staging YML files, etc.). This suggests either: (a) the codebase is in a stable/frozen state, or (b) the 30-day commit history data is incomplete. **Insufficient information to answer which files changed most frequently in the last 90 days.**

---
## Quick-Reference Architecture

**Top modules (by architectural centrality):**
- `.pre-commit-config.yaml` (pagerank=0.0455)
- `.github/workflows/scripts/dbt_cloud_run_job.py` (pagerank=0.0455)
  _This script automates the execution of a dbt job on the dbt Cloud platform. It triggers a job, monitors its status, and _
- `packages.yml` (pagerank=0.0455)
- `Taskfile.yml` (pagerank=0.0455)
- `package-lock.yml` (pagerank=0.0455)

**Data entry points:**
- `renamed`
- `source`
- `/home/neba/tenx/week4/repos/jaffle-shop/models/marts/metricflow_time_spine.sql:metricflow_time_spine`
- `joined`
- `order_supplies_summary`

**Data output endpoints:**
- `metricflow_time_spine`

---
## Evidence: Module Citation Index
- `.pre-commit-config.yaml` (no metadata)
- `.github/workflows/scripts/dbt_cloud_run_job.py` (domain=monitoring)
- `packages.yml` (no metadata)
- `Taskfile.yml` (no metadata)
- `package-lock.yml` (no metadata)
- `dbt_project.yml` (no metadata)
- `.github/workflows/cd_prod.yml` (no metadata)
- `.github/workflows/cd_staging.yml` (no metadata)
- `.github/workflows/ci.yml` (no metadata)
- `models/staging/stg_customers.yml` (no metadata)
