# FDE Day-One Onboarding Brief

_Generated by Brownfield Cartographer — 2026-03-13 15:34 UTC_

---
## The Five FDE Day-One Questions

### Q1: What is the primary data ingestion path?

The primary data ingestion path appears to be through the `models/staging/stg_customers.yml`, `models/staging/stg_products.yml`, `models/staging/stg_locations.yml`, `models/staging/stg_supplies.yml`, and `models/staging/stg_orders.yml` files. These files are high-velocity and indicate that they are being frequently updated.

### Q2: What are the 3-5 most critical output datasets/endpoints?

The most critical output datasets/endpoints are the marts, which include:
- `models/marts/order_items.yml`
- `models/marts/products.yml`
- `models/marts/supplies.yml`
- `models/marts/locations.yml`
- `models/marts/customers.yml`
- `models/marts/orders.yml`

### Q3: What is the blast radius if the most critical module fails?

The blast radius if the most critical module fails would depend on which marts are affected. Given the architecture, if any of the marts fail, the impact could be widespread since many parts of the system might rely on these marts for data. However, without more specific information on how the marts interconnect, it's challenging to pinpoint the exact blast radius.

### Q4: Where is the business logic concentrated vs distributed?

The business logic is concentrated in the marts, specifically in files like `models/marts/order_items.yml`, `models/marts/products.yml`, `models/marts/supplies.yml`, `models/marts/locations.yml`, `models/marts/customers.yml`, and `models/marts/orders.yml`. These files contain the business logic for creating meaningful metrics and summaries from the raw data.

### Q5: What has changed most frequently in the last 90 days (high-velocity files)?

The files that have changed most frequently in the last 90 days (high-velocity files) are listed as:
- `packages.yml`
- `Taskfile.yml`
- `package-lock.yml`
- `dbt_project.yml`
- `.github/workflows/cd_prod.yml`
- `.github/workflows/cd_staging.yml`
- `.github/workflows/ci.yml`
- `models/staging/stg_customers.yml`
- `models/staging/stg_products.yml`
- `models/staging/stg_locations.yml`

However, the architectural context provided indicates that none of these files have any commits in the last 30 days. Therefore, based on the provided information, no files have changed in the last 90 days.

---
## Quick-Reference Architecture

**Top modules (by architectural centrality):**
- `packages.yml`
- `Taskfile.yml`
- `package-lock.yml`
- `dbt_project.yml`
- `.github/workflows/cd_prod.yml`

**Data entry points:**
- `renamed`
- `source`
- `/home/neba/tenx/week4/jaffle-shop/models/marts/metricflow_time_spine.sql:metricflow_time_spine`
- `order_supplies_summary`
- `joined`

**Data output endpoints:**
- `stg_locations`
- `stg_orders`
- `stg_order_items`
- `stg_products`
- `stg_supplies`
