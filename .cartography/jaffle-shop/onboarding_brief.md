# FDE Day-One Onboarding Brief

_Generated by Brownfield Cartographer — 2026-03-14 14:37 UTC_

---
## The Five FDE Day-One Questions

### Q1: What is the primary data ingestion path?

What is the primary data ingestion path?**

The primary data ingestion path flows through source definitions into staging models. Raw sources are defined in `models/staging/__sources.yml`, which feed into staging transformation models (`stg_customers.yml`, `stg_products.yml`, `stg_locations.yml`, `stg_supplies.yml`, `stg_orders.yml`, `stg_order_items.yml`). The architecture uses dbt's source abstraction layer to ingest raw data, as evidenced by the data lineage entry point named "source" and the high-PageRank staging layer files.

**

### Q2: What are the 3-5 most critical output datasets/endpoints?

What are the 3-5 most critical output datasets/endpoints?**

The critical output datasets (data lineage sinks with out-degree=0) are:
1. `stg_customers` (staging layer)
2. `stg_orders` (staging layer)
3. `stg_order_items` (staging layer)
4. `metricflow_time_spine` (`models/marts/metricflow_time_spine.sql`) — serving as a time dimension for metrics
5. The marts layer aggregations: `customers.yml`, `products.yml`, `supplies.yml`, `locations.yml`, `order_items.yml` (in `models/marts/`)

**

### Q3: What is the blast radius if the most critical module fails?

What is the blast radius if the most critical module fails?**

If `dbt_project.yml` (PageRank=0.0455, architectural hub) fails, the entire dbt DAG orchestration collapses — no models execute. If the CI/CD automation in `.github/workflows/scripts/dbt_cloud_run_job.py` fails, deployments to both staging (`cd_staging.yml`) and production (`cd_prod.yml`) halt. However, the architectural context shows **no circular dependencies detected**, limiting cascading failures. The staging models (`stg_*.yml`) are foundational; their failure blocks all downstream marts (`models/marts/*.yml`).

**

### Q4: Where is the business logic concentrated vs distributed?

Where is the business logic concentrated vs distributed?**

**Concentrated:** dbt transformation logic is centralized in the dbt project structure (`dbt_project.yml`, `packages.yml`). CI/CD orchestration logic is concentrated in `.github/workflows/scripts/dbt_cloud_run_job.py`.

**Distributed:** Business logic is distributed across the layered architecture — staging transformations spread across `models/staging/stg_*.yml` files, and mart-level aggregations/joins distributed across `models/marts/*.yml` (customers, products, supplies, locations, order_items). Data lineage entry points (e.g., `order_supplies_summary`, `customer_orders_summary`, `customer_order_count`, `compute_booleans`) suggest logic scattered across multiple derivative datasets.

**

### Q5: What has changed most frequently in the last 90 days (high-velocity files)?

What has changed most frequently in the last 90 days (high-velocity files)?**

The architectural context shows **0 commits across all tracked files in the last 30 days**, indicating either a frozen/stable codebase or incomplete commit history data. Therefore, **I cannot definitively identify high-velocity files from the provided dataset.** The context does not contain sufficient granularity (90-day window unavailable; only 30-day data provided with zero commits) to answer this question reliably.

---
## Quick-Reference Architecture

**Top modules (by architectural centrality):**
- `.github/workflows/scripts/dbt_cloud_run_job.py` (pagerank=0.0455)
  _This Python script automates the process of triggering and monitoring a dbt job in a CI/CD pipeline. It uses environment_
- `packages.yml` (pagerank=0.0455)
- `Taskfile.yml` (pagerank=0.0455)
- `package-lock.yml` (pagerank=0.0455)
- `dbt_project.yml` (pagerank=0.0455)

**Data entry points:**
- `renamed`
- `source`
- `/home/neba/tenx/week4/repos/jaffle-shop/models/marts/metricflow_time_spine.sql:metricflow_time_spine`
- `order_supplies_summary`
- `joined`

**Data output endpoints:**
- `stg_locations`
- `stg_orders`
- `stg_order_items`
- `stg_products`
- `stg_supplies`

---
## Evidence: Module Citation Index
- `.github/workflows/scripts/dbt_cloud_run_job.py` (domain=monitoring)
- `packages.yml` (no metadata)
- `Taskfile.yml` (no metadata)
- `package-lock.yml` (no metadata)
- `dbt_project.yml` (no metadata)
- `.github/workflows/cd_prod.yml` (no metadata)
- `.github/workflows/cd_staging.yml` (no metadata)
- `.github/workflows/ci.yml` (no metadata)
- `models/staging/stg_customers.yml` (no metadata)
- `models/staging/stg_products.yml` (no metadata)
