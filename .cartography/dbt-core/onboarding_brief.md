# FDE Day-One Onboarding Brief

_Generated by Brownfield Cartographer — 2026-03-14 21:13 UTC_

---
## The Five FDE Day-One Questions

### Q1: What is the primary data ingestion path?

What is the primary data ingestion path?**

The primary data ingestion path flows through YAML schema file parsing. Configuration enters via `core/dbt/contracts/project.py` (project metadata, dependencies, quoting rules), then proceeds through `core/dbt/parser/sources.py` for source definitions and `core/dbt/parser/common.py` for generic YAML block parsing into typed node definitions. The entry points are dbt project files (`dbt_project.yml`, `profiles.yml`) located by `core/dbt/cli/resolvers.py`. Environment-specific rendering occurs in `core/dbt/config/renderer.py` with secret handling via `core/dbt/context/secret.py`.

**

### Q2: What are the 3-5 most critical output datasets/endpoints?

What are the 3-5 most critical output datasets/endpoints?**

Based on the architectural hub analysis and data lineage sinks:

1. **Manifest artifact** – serialized project graph state, uploaded via `core/dbt/utils/artifact_upload.py` to dbt Cloud
2. **Run results** (sinks: `t`, `snapshot`, `incremental`, `metricflow_time_spine`) – model execution outcomes and compiled SQL
3. **Structured event logs** – telemetry events sent via `core/dbt/tracking.py` to Snowplow collector
4. **Exit codes/CLI results** – wrapped via `core/dbt/cli/exceptions.py` (`ResultExit`, `ExceptionExit`) to shell
5. **Execution graph state** – managed by `core/dbt/graph/queue.py` and `core/dbt/graph/graph.py` for DAG traversal

**

### Q3: What is the blast radius if the most critical module fails?

What is the blast radius if the most critical module fails?**

If `core/dbt/node_types.py` (highest PageRank=0.0482) fails, the **blast radius is entire execution engine**. This module's categorized node type lists are the foundation for filtering executable, referenceable, testable, and versioned nodes. Failure cascades to:
- `core/dbt/graph/graph.py` (depends on node categorization for traversal)
- `core/dbt/graph/selector_methods.py` (uses node types for filtering)
- `core/dbt/parser/common.py` (validates nodes against type definitions)
- `core/dbt/task/function.py` (checks node types before execution)

The second-most critical `core/dbt/exceptions.py` (PageRank=0.0371) would disable error reporting/handling across all domains, preventing graceful failure modes.

**

### Q4: Where is the business logic concentrated vs distributed?

Where is the business logic concentrated vs distributed?**

**Concentrated:**
- **Parsing logic**: `core/dbt/parser/sources.py`, `core/dbt/parser/common.py` (YAML→object conversion)
- **Configuration management**: `core/dbt/contracts/project.py`, `core/dbt/config/renderer.py` (Jinja rendering + secret masking)
- **Graph operations**: `core/dbt/graph/graph.py`, `core/dbt/graph/queue.py` (DAG scheduling and traversal)

**Distributed:**
- **Node execution**: `core/dbt/task/function.py` (adapter-specific materialization), test fixtures scattered across `tests/functional/semantic_models/`
- **Error handling**: Domain-specific exceptions across `core/dbt/exceptions.py`, `core/dbt/cli/exceptions.py`
- **Selection logic**: `core/dbt/graph/selector_methods.py` implements plugin architecture with method classes for different criteria (FQN, tags, paths, state)

**

### Q5: What has changed most frequently in the last 90 days (high-velocity files)?

What has changed most frequently in the last 90 days (high-velocity files)?**

Six files represent active development areas:

1. **Semantic model v2 parsing** (`tests/functional/semantic_models/test_semantic_model_v2_parsing.py` – 6 commits) – new semantic layer feature
2. **Semantic model fixtures** (`tests/functional/semantic_models/fixtures.py` – 5 commits) – test support for semantic v2
3. **Compilation pipeline** (`core/dbt/compilation.py` – 4 commits) – core execution logic refactoring
4. **CLI parameters** (`core/dbt/cli/params.

---
## Quick-Reference Architecture

**Top modules (by architectural centrality):**
- `core/dbt/node_types.py` (pagerank=0.0482)
  _This module defines categorized lists of node types that classify different DAG node operations in dbt's execution model_
- `core/dbt/exceptions.py` (pagerank=0.0371, 1 commits/30d)
  _This module defines a comprehensive set of domain-specific exception classes for dbt's core operations, including contra_
- `tests/__init__.py` (pagerank=0.0277)
  _This file serves as a Python package marker for the tests directory, enabling the test suite to be organized as a proper_
- `core/dbt/tracking.py` (pagerank=0.0061)
  _This module implements anonymous usage telemetry collection for dbt by wrapping the Snowplow tracking library to send st_
- `core/dbt/tests/fixtures/project.py` (pagerank=0.0057, 1 commits/30d)
  _This module provides pytest fixtures that establish isolated dbt project environments for functional testing, including _

**Data entry points:**
- `source_data`
- `/home/neba/tenx/week4/repos/dbt-core/tests/functional/fixtures/happy_path_project/analyses/a.sql:a`
- `/home/neba/tenx/week4/repos/dbt-core/tests/functional/fixtures/happy_path_project/tests/t.sql:t`
- `/home/neba/tenx/week4/repos/dbt-core/tests/functional/fixtures/happy_path_project/functions/area_of_circle.sql:area_of_circle`
- `/home/neba/tenx/week4/repos/dbt-core/tests/functional/fixtures/happy_path_project/models/metricflow_time_spine.sql:metricflow_time_spine`

**Data output endpoints:**
- `t`
- `area_of_circle`
- `snapshot`
- `expression_is_true`
- `metricflow_time_spine`

---
## Evidence: Module Citation Index
- `core/dbt/node_types.py` (domain=test fixtures)
- `core/dbt/exceptions.py` (1 commits/30d, domain=test fixtures)
- `tests/__init__.py` (domain=test fixtures)
- `core/dbt/tracking.py` (domain=test fixtures)
- `core/dbt/tests/fixtures/project.py` (1 commits/30d, domain=test fixtures)
- `core/dbt/contracts/project.py` (1 commits/30d, domain=test fixtures)
- `core/dbt/config/renderer.py` (domain=test fixtures)
- `core/dbt/cli/exceptions.py` (domain=test fixtures)
- `core/dbt/parser/common.py` (domain=test fixtures)
- `core/dbt/cli/resolvers.py` (domain=test fixtures)
