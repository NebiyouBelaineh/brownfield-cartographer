# FDE Day-One Onboarding Brief

_Generated by Brownfield Cartographer — 2026-03-14 14:54 UTC_

---
## The Five FDE Day-One Questions

### Q1: What is the primary data ingestion path?

What is the primary data ingestion path?

**Answer:** The primary data ingestion path flows through **Airbyte** as the ELT ingestion layer, which is orchestrated by `dg_projects/lakehouse/lakehouse/definitions.py`. This module "conditionally loads Airbyte connections based on environment configuration" and creates "scheduled sync-and-stage jobs for different data groups." The ingested data then flows into dbt transformation models (evidenced by the high-velocity file `src/ol_dbt/models/reporting/_reporting__models.yml` with 8 commits in 30d). Secondary ingestion points include tracking logs (via `bin/utils/chunk_tracking_logs_by_day.py`) and external source tables referenced in the data lineage sources (e.g., `studentmodule_table`), but Airbyte→dbt is the primary orchestrated path.

##

### Q2: What are the 3-5 most critical output datasets/endpoints?

What are the 3-5 most critical output datasets/endpoints?

**Answer:** Based on data lineage sinks (outputs with out-degree=0), the critical output datasets are:

1. **`edxorg_to_mitxonline_users`** – User identity mapping transformation
2. **`edxorg_to_mitxonline_enrollments`** – Enrollment event transformation
3. **`problem_engagement_detail_report`** – Reporting dataset for engagement analytics
4. **`learner_demographics_and_cert_info`** – Learner attribute and certification reporting
5. **`edxorg_to_mitxonline_program_entitlements`** – Program access control data

These are defined as dbt sinks in the transformation DAG and feed downstream reporting (evidenced by Superset integration in `dg_projects/lakehouse/lakehouse/definitions.py`).

##

### Q3: What is the blast radius if the most critical module fails?

What is the blast radius if the most critical module fails?

**Answer:** The most critical module is `packages/ol-orchestrate-lib/src/ol_orchestrate/lib/postgres/event_log.py` (pagerank=0.0022, second-highest architectural hub; 4 commits in 30d). **Blast radius: CRITICAL – system-wide orchestration failure.** This module provides "PostgreSQL-backed event log storage implementation for Dagster" with "configurable connection pooling parameters." If it fails, Dagster cannot persist job execution events, causing:
- Loss of job execution history and auditing
- Inability to track retry logic or failure propagation
- Potential connection exhaustion cascading to all dependent orchestration (b2b_organization, lakehouse, reconciliation jobs)
- Complete blockage of all downstream data pipelines that depend on Dagster event persistence

Secondary blast radius: `dg_projects/lakehouse/lakehouse/definitions.py` (pagerank=0.0017) orchestrates the entire ELT pipeline; its failure blocks all Airbyte→dbt→Superset workflows.

##

### Q4: Where is the business logic concentrated vs distributed?

Where is the business logic concentrated vs distributed?

**Answer:**

**Concentrated (Hub modules):**
- **Data transformation logic** is concentrated in `dg_projects/lakehouse/lakehouse/definitions.py` and `src/ol_dbt/models/` (dbt models), which orchestrates all Airbyte ingestion and dbt transformations in a unified pipeline
- **B2B organization export logic** is concentrated in `dg_projects/b2b_organization/b2b_organization/definitions.py` with its sensor (`dg_projects/b2b_organization/b2b_organization/sensors/b2b_organization.py`) driving partition-aware exports

**Distributed:**
- **Partition management** is split across multiple partition definition files (e.g., `dg_projects/b2b_organization/b2b_organization/partitions/b2b_organization.py`)
- **Data reconciliation logic** is distributed: `dg_deployments/reconcile_edxorg_partitions.py` handles S3 path correction independently, while `bin/utils/chunk_tracking_logs_by_day.py` manages log reorganization as a separate script
- **Development automation** is scattered across multiple bin scripts (`bin/dbt-local-dev.py`, `bin/uv-operations.py`, `bin/dbt-create-staging-models.py`)

##

### Q5: What has changed most frequently in the last 90 days (high-velocity files)?

What has changed most frequently in the

---
## Quick-Reference Architecture

**Top modules (by architectural centrality):**
- `dg_projects/b2b_organization/__init__.py` (pagerank=0.0031)
- `packages/ol-orchestrate-lib/src/ol_orchestrate/lib/postgres/event_log.py` (pagerank=0.0022, 4 commits/30d)
  _This module provides a PostgreSQL-backed event log storage implementation for Dagster that replaces the default NullPool_
- `dg_deployments/reconcile_edxorg_partitions.py` (pagerank=0.0017, 6 commits/30d)
  _This script corrects a data-integrity bug in Dagster's edxorg course asset partitioning system where course IDs were inc_
- `dg_projects/__init__.py` (pagerank=0.0017)
- `bin/dbt-create-staging-models.py` (pagerank=0.0017)
  _This script automates the generation of dbt data transformation artifacts by discovering database tables matching a spec_

**Data entry points:**
- `needs_deduplication`
- `endfor`
- `endmacro`
- `the`
- `source_node`

**Data output endpoints:**
- `generate_base_model_enhanced`
- `generate_model_yaml_enhanced`
- `transform_studentmodule_data`
- `apply_deduplication_query`
- `edxorg_to_mitxonline_users`

---
## Evidence: Module Citation Index
- `dg_projects/b2b_organization/__init__.py` (domain=unknown)
- `packages/ol-orchestrate-lib/src/ol_orchestrate/lib/postgres/event_log.py` (4 commits/30d, domain=asset persistence infrastructure)
- `dg_deployments/reconcile_edxorg_partitions.py` (6 commits/30d, domain=risk scoring pipeline)
- `dg_projects/__init__.py` (domain=unknown)
- `bin/dbt-create-staging-models.py` (domain=dbt-superset integration)
- `bin/dbt-local-dev.py` (1 commits/30d, domain=dbt-superset integration)
- `bin/uv-operations.py` (domain=asset persistence infrastructure)
- `bin/utils/chunk_tracking_logs_by_day.py` (domain=asset persistence infrastructure)
- `dg_projects/student_risk_probability/__init__.py` (domain=unknown)
- `dg_projects/b2b_organization/b2b_organization_tests/__init__.py` (domain=unknown)
